var firstName = 'Himanshu';
var lastName = 'Gawari';
var age = 30;
var first = true;
var last = false;

// falsey values
// false, null, undefined, 0, "", '', NaN

if (age <= 40) {
  // if age is less than or equal to 30
} else if (age > 40 && age < 50) {
  // if age is greater than 40 and less than 50
  // && - AND
  // || - OR
} else if (age < 0) {
  // prevent something from happening
} else {
  // if age greater than 50
}

// Comparison Operators
// < <= > >= == === != !== !
